spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/prompt_testing
    username: promptuser
    password: promptpass

  data:
    redis:
      host: localhost
      port: 6379
      timeout: 2000ms

ai:
  mock-mode: ${AI_MOCK_MODE:true}  # 配置真实模型凭据后可切换为 false。
  openai:
    api-key: ${OPENAI_API_KEY:}
  anthropic:
    api-key: ${ANTHROPIC_API_KEY:}
