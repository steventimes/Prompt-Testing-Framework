spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/prompt_testing
    username: promptuser
    password: promptpass
  
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 2000ms

ai:
  mock-mode: true  # Set to false when have real API keys
# TODO, add api keys later
# Only needed when mock-mode is false
# langchain4j:
#   open-ai:
#     chat-model:
#       api-key: ${OPENAI_API_KEY:}
#   anthropic:
#     chat-model:
#       api-key: ${ANTHROPIC_API_KEY:}